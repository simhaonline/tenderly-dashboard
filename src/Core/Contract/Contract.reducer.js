const initialState = {
    contracts: {},
    contractStatus: {},
};

const ContractReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default ContractReducer;
